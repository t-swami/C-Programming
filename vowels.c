#include<stdio.h>
int main(){
   char x;
   printf("Enter the character:");
   scanf("%c",&x);
   switch(x)
   {
   	case 'a':
   		printf("%c is vowel",x);
   		break;
   	case 'e':
   		printf("%c is vowel",x);
   		break;
   	case 'i':
   		printf("%c is vowel",x);
   		break;
   	case 'o':
   		printf("%c is vowel",x);
   		break;
   	case 'u':
   		printf("%c is vowel",x);
   		break;
   	case 'A':
   		printf("%c is vowel",x);
   		break;
   	case 'E':
   		printf("%c is vowel",x);
   		break;
   	case 'I':
   		printf("%c is vowel",x);
   		break;
   	case 'O':
   		printf("%c is vowel",x);
   		break;
   	case 'U':
   		printf("%c is vowel",x);
   		break;
   	default:
   		printf("%c is a con",x);
   		break;
   		
	   }
	   return 0;	
}

